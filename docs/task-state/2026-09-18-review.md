# 2026-09-18 审查与本地验收

## 授权与范围

用户要求“审查下并继续完成”，后续确认 GitHub 已登录。在既有 v0.1 范围内修复缺陷、补回归、验证安装/示例/打包、完善 CI，并创建公开仓库。

## 来源与验收

- 产品目标和阶段验收：`plans/00_总目标.md`、`plans/12` 至 `plans/19`。
- 当前实现：`src/crossrepro`、`tests`、`examples`。
- `VALIDATION_REPORT.md` 的 75 PASS / 93% 是 2026-09-17 的历史记录，不代表修改后结果。
- 本地验收：完整 pytest、实际三示例、collect/replay/pack/CI 闭环、ZIP 隐私与校验和、构建和隔离安装。
- 外部验收：真实 Windows/Linux/macOS GitHub-hosted runners 已完成；正式 release 仍需在 release-version workflow 通过后创建。

## 实施顺序

1. 保存基线并在本机运行原始测试。
2. 修复 PowerShell 退出码/参数语义、报告脱敏、打包扫描/协议校验。
3. 审查关联路径、Schema、CI 和测试边界，补充有独立预期的回归。
4. 完成本地验证、更新文档、构建新开发 wheel，核对最终差异。

## 证据与当前状态

- 原始快照：`../build_artifacts/review-baseline-20260918.zip`。
- 原始文件哈希：`../build_artifacts/review-baseline-sha256.json`。
- 原始 Windows 测试结果：`../build_artifacts/baseline-windows-tests.*`（运行结果待核对）。
- 项目隔离环境：`.venv`，Python 3.12.14，依赖按 `pyproject.toml` 安装；缓存位于 F 盘。
- 已在只读检查中复现 PowerShell 将示例退出码 3/2 变成 1，以及报告命令/失败原因中假密钥残留。
- 当前状态：PUBLIC_REPOSITORY / HOSTED_MATRIX_VALIDATED / FINAL_CI_GATE。

## 已完成与证据

- 原始 Windows 基线：72 通过、3 失败、2 条解码警告。
- 首批新增 18 项回归：修复前全部失败，证据 `../build_artifacts/regressions-before.*`。
- 发布候选源码：0.1.0；完整 pytest 111 通过、1 跳过（当前 `.pytest_cache` 没有写权限，仅产生警告），此前覆盖率 814/903 = 90.14%。
- 跳过项：主机不允许创建真实符号链接。该项仍需支持链接的真实 runner 验证。
- 最终测试：`../build_artifacts/release-0.1.0-windows-final-tests.*`。
- Windows 三示例真实结果：REPRODUCED(3)、ENVIRONMENT_BLOCKED、REPRODUCED(2)。
- 闭环及 ZIP 验证：`.validation/windows-dev2-final/validation.json`；假密钥扫描、完整 SHA256、manifest 校验通过。
- 发布候选 wheel：`../build_artifacts/release-0.1.0/crossrepro-0.1.0-py3-none-any.whl`，SHA-256 为 `cd676c6808d3f4534b6245f627ba1d28ff3d7933a2849563ad4a83e8aed17617`；26 个打包 Python 文件已与源码逐字节比对，并在干净 venv 安装、独立导入及真实采集重放。
- CI 已补 Summary、真实例子、打包检查和干净安装；`https://github.com/taopeng9192/crossrepro/actions/runs/35338776666` 的六个 Windows/Ubuntu/macOS × Python 3.10/3.12 job 全部成功。每份 artifact 均有 112 测试、零失败/错误；Linux 两个 job 显式跳过符号链接用例，Windows/macOS 无跳过。三示例、隐私扫描、SHA-256、wheel 与干净安装检查均通过。
- 已创建并推送公开仓库 `https://github.com/taopeng9192/crossrepro`，初始提交为 `34524cbfe89ba158865b7156351f8b1d1b59b8fe`。未修改全局环境配置；项目 `.venv` 中缺失的 pip 已通过 Python 内置 `ensurepip` 恢复。
- version/release 文档提交 `86bc39042ab9dbabb028cf206c5a1d70d54c7d19` 的 GitHub-hosted 三平台矩阵已通过：`https://github.com/taopeng9192/crossrepro/actions/runs/35339802773`。下载的六份 JUnit 报告合计 672 次测试执行，零 failures/errors；两个 Linux runner 各有一个显式 symlink skip。
- 已创建 annotated tag `v0.1.0`，其远端 peeled ref 指向该 version/release 文档提交；正式 Release 为 `https://github.com/taopeng9192/crossrepro/releases/tag/v0.1.0`。附带 wheel 的 SHA-256 为 `cd676c6808d3f4534b6245f627ba1d28ff3d7933a2849563ad4a83e8aed17617`。
- wheel 已从公开 Release URL 下载到新建隔离 venv，安装版本 `0.1.0`，并验证 `crossrepro --help` 成功。

## 下一步及阻塞

1. 阶段 10 选择公开、可本地复现且有实际维护价值的 OSS issue，并保留透明的 case 记录；不要批量向上游发帖。
2. 真实 OSS 试用、维护反馈、issue triage、Codex 集成与申请仍按原计划推进；目前没有外部使用或反馈可据此声称达成。

安装过程中发现主机 pip 默认 `--user` 与 venv 冲突，已仅在 wheel 验证子进程中禁用配置/明确 `--no-user`；最终验证成功。构建仍有已有许可证元数据格式的 setuptools 弃用警告，未擅自升级构建工具链。
